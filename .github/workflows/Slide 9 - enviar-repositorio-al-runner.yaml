name: Enviar Repositorio al Runner
on: [push]

jobs:
  checkout-action:
    runs-on: ubuntu-latest
    steps: 
      - name: Checkout de archivos hacia runner
        uses: actions/checkout@v6
      - name: Mostrar los archivos traídos
        run: ls -la $GITHUB_WORKSPACE